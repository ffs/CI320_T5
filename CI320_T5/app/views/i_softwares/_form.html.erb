<%= form_for(@i_software) do |f| %>
  <% if @i_software.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@i_software.errors.count, "error") %> prohibited this i_software from being saved:</h2>

      <ul>
      <% @i_software.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :i_software_name %><br>
    <%= f.text_field :i_software_name %>
  </div>
  <div class="field">
    <%= f.label :version %><br>
    <%= f.text_field :version %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
